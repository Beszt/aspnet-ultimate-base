name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version in format X.Y.Z (ex. 0.1.0)'
        required: true
        type: string
      version_name:
        description: 'Release name (e.g. Docker)'
        required: true
        type: string
      publish_docker:
        description: 'Build and push Docker image'
        required: false
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    environment: Production
    env:
      DOCKERHUB_USERNAME: ${{ vars.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
      DOCKERHUB_REPOSITORY: ${{ vars.DOCKERHUB_REPOSITORY }}
    steps:
      - name: Checkout develop
        uses: actions/checkout@v4
        with:
          ref: develop
          fetch-depth: 0

      - name: Basic version sanity check
        run: |
          if ! [[ "${{ github.event.inputs.version }}" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Version must be X.Y.Z"; exit 1; fi

      - name: Create release branch
        run: |
          REL="release/${{ github.event.inputs.version }}"
          git switch -c "$REL"
          git push --set-upstream origin "$REL"

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Update changelog
        run: python .github/workflows/tools/update_changelog.py "${{ github.event.inputs.version }}" "${{ github.event.inputs.version_name }}"

      - name: Update app version
        run: python .github/workflows/tools/update_app_version.py "${{ github.event.inputs.version }}"

      - name: Commit and push release updates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add documentation/CHANGELOG.md AspNetUltimateBase.Presentation/VersionInfo.cs
          if git diff --cached --quiet; then
            echo "No release changes detected; aborting."
            exit 1
          fi
          git commit -m "[workflow-release] v${{ github.event.inputs.version }} - ${{ github.event.inputs.version_name }}"
          git push

      - name: Verify release files
        run: python .github/workflows/tools/verify_release_readiness.py "${{ github.event.inputs.version }}" "${{ github.event.inputs.version_name }}"

      - name: Restore
        run: dotnet restore AspNetUltimateBase.sln

      - name: Build (Release)
        run: dotnet build AspNetUltimateBase.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test AspNetUltimateBase.sln --configuration Release --no-build

      - name: Publish API (Release)
        run: dotnet publish AspNetUltimateBase.Presentation/AspNetUltimateBase.Presentation.csproj --configuration Release --no-restore -o out/publish

      - name: Verify Docker credentials
        if: ${{ github.event.inputs.publish_docker == 'true' }}
        run: |
          if [ -z "${DOCKERHUB_USERNAME}" ] || [ -z "${DOCKERHUB_TOKEN}" ] || [ -z "${DOCKERHUB_REPOSITORY}" ]; then
            echo "Docker publish requested but DOCKERHUB_* secrets are missing.";
            exit 1;
          fi

      - name: Set up Docker Buildx
        if: ${{ github.event.inputs.publish_docker == 'true' }}
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        if: ${{ github.event.inputs.publish_docker == 'true' }}
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ env.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        id: docker_push
        if: ${{ github.event.inputs.publish_docker == 'true' }}
        continue-on-error: true
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.DOCKERHUB_REPOSITORY }}:${{ github.event.inputs.version }}
            ${{ env.DOCKERHUB_REPOSITORY }}:latest

      - name: Abort release when Docker publish fails
        if: ${{ github.event.inputs.publish_docker == 'true' && steps.docker_push.outcome == 'failure' }}
        run: |
          echo 'Docker build/push failed; aborting release flow.'
          exit 1

      - name: Zip artifact
        if: ${{ github.event.inputs.publish_docker != 'true' || steps.docker_push.outcome == 'success' }}
        run: |
          mkdir -p out/releases
          cd out/publish
          zip -r ../releases/aspnet-ultimate-base-${{ github.event.inputs.version }}.zip .
          cd ../..

      - name: Create tag
        if: ${{ github.event.inputs.publish_docker != 'true' || steps.docker_push.outcome == 'success' }}
        run: |
          git tag "v${{ github.event.inputs.version }}"
          git push origin "v${{ github.event.inputs.version }}"

      - name: Create GitHub Release
        if: ${{ github.event.inputs.publish_docker != 'true' || steps.docker_push.outcome == 'success' }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.event.inputs.version }}
          name: v${{ github.event.inputs.version }}
          draft: false
          prerelease: false
          files: |
            out/releases/aspnet-ultimate-base-${{ github.event.inputs.version }}.zip
